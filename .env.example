# Database (PostgreSQL) - Required
DATABASE_URL="postgresql://user:password@host:5432/database_name"

# JWT Secret - Required (generate a strong random string)
JWT_SECRET="your-super-secret-random-string-min-32-chars"

# App URL - Required for production
NEXT_PUBLIC_APP_URL="https://your-domain.vercel.app"

# Socket Service (optional, for real-time updates)
# If not set, the app works without live dashboard updates
NEXT_PUBLIC_SOCKET_URL="https://your-socket-service.onrender.com"
SOCKET_SERVER_URL="https://your-socket-service.onrender.com"

# Socket relay secret - Required whenever a socket service is used
# Shared by the Next server (the only side allowed to emit) and the mini-service.
# Generate a strong random string. Without it the mini-service runs listen-only
# and refuses every relay event, so dashboards stop live-updating.
SOCKET_RELAY_TOKEN="your-super-secret-random-string-min-32-chars"

# Browser origins allowed to open a socket (optional, comma-separated)
# Defaults to NEXT_PUBLIC_APP_URL plus http://localhost:3000. Add your LAN or
# staging origins here if you open the app from another host.
# SOCKET_ALLOWED_ORIGINS="https://app.yourdomain.com,http://192.168.1.10:3000"

# School Domains (optional, for subdomain routing)
# Format: hostname=CODE,hostname2=CODE2
# SCHOOL_DOMAINS="shb-001.yourdomain.com=SHB-001"
